CREATE OR REPLACE FUNCTION TEL_INFO_F
(TEL_DATA IN VARCHAR2)
RETURN VARCHAR2
IS
TEL VARCHAR2(20);
BEGIN
TEL := '010-' ||
       SUBSTR(TEL_DATA,1,4) ||'-' ||
       SUBSTR(TEL_DATA,5);

RETURN TEL;
END;
/
